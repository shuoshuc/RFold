The MLOCS project leverages OCS to improve the ML cluster performance, reliability, cost and power consumption.

# Requirements

WorkloadGen requires Python packages `scipy`.
```
pip install 'scipy>=1.13.1'
```